spring.application.name=contact-app
# src/main/resources/application.properties
# Spring Boot should automatically pick up the @Component filter, 
# but setting the base path is good practice.

# Optional: Set a default role for testing
spring.security.user.roles=SUPER_ADMIN,BUSINESS_UNIT_ADMIN,BUSINESS_UNIT_MEMBER
